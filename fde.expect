#! /usr/bin/env expect -df

set PASSPHRASE [sudo ./de_kc.py /etc/kcpassword]

# Cargo culting: people recommend this; not sure how necessary
set send_slow {1 .1}
spawn sudo fdesetup enable -user $USER
expect "Password:"
send "$PASSPHRASE\r"
expect "Enter the password for user '$USER':"
send "$PASSPHRASE\r"
expect eof
