#! /usr/bin/env expect -df
# log_user 0
set PASSPHRASE [exec sudo ./de_kc.py]
set USER [lindex $argv 0];

# Cargo culting: people recommend this; not sure how necessary
set send_slow {1 .1}
spawn sudo ./heckinheck enable -user $USER
# expect "Password:"
# send "$PASSPHRASE\r"
expect "Enter the password for user '$USER':"
send "$PASSPHRASE\r"
expect "Enter the password for user '$USER':"
# Enter the password for user 'travis':
send "$PASSPHRASE\r"
expect eof
