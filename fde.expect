#! /usr/bin/env expect -df
log_user 0
set stty_init -echo
set PASSPHRASE [exec sudo ./de_kc.py]
set USER [lindex $argv 0];

# Cargo culting: people recommend this; not sure how necessary
set send_slow {1 .1}
spawn fdesetup enable -user $USER
# expect "Password:"
# send "$PASSPHRASE\r"
expect "Enter the password for user*"
send "$PASSPHRASE\r"
expect "Enter the password for user*"
# Enter the password for user 'travis':
send "$PASSPHRASE\r"
expect eof
